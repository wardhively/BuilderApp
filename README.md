# BuilderApp

This is a minimal prototype for a Builder portal. It exposes a small REST API and a simple OpenStreetMap-based page to display orders on a map.

## Setup

```bash
npm install
```

## Running the Server

```bash
node index.js
```

Then navigate to `http://localhost:3000` to see the map.

## Tests

```bash
npm test
```
